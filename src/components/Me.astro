---
---

<section id="hero" class="me">
  <div class="main">
    <h1>SANTI</h1>

    <h3>I'M READY TO CREATE AWESOME WEB EXPERIENCES, FROM DESIGN TO CODE!</h3>
    <h3 id="back">
      I'M READY TO CREATE AWESOME WEB EXPERIENCES, FROM DESIGN TO CODE!
    </h3>

    <div class="img">
      <img
        src="images/henrique-santi.webp"
        alt="henrique-santi"
        class="cover"
      />
    </div>
  </div>
</section>

<style lang="scss">
  .me {
    height: 80vh;
    width: 100%;
    margin: 0;
    padding: 0;
    display: flex;
    justify-content: center;
    background-color: var(--clr-floral-dark);
    .main {
      height: 100%;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      margin: 0 auto;

      h1 {
        font-size: var(--fs-900);
        color: var(--clr-dark-bone);
        width: 100%;
        text-align: center;
        position: absolute;
        top: 14%;
        left: 50%;
        transform: translate(-50%, -50%);
        
      }

      h3 {
        width: 100%;
        font-size: 4.2rem;
        text-align: center;
        z-index: 100;
        color: var(--clr-bone);
        font-size: var(--fs-500);
        mix-blend-mode: soft-light;
        position: absolute;
        top: 40%;
        left: 50%;
        transform: translate(-50%, -50%);
      }
      #back {
        color: var(--clr-floral-dark);
        z-index: 40;
        mix-blend-mode: normal;
      }

      .img {
        max-width: 12.5rem;
        img {
          max-width: 12.5rem;
          display: block;
          position: absolute;
          border-radius: 0.4rem;
          top: 40%;
          left: 50%;
          transform: translate(-50%, -50%) rotate(12deg);
          z-index: 80;
        }
      }
    }
  }

  @media (min-width: "390px") {
    //
  }

  @media (min-width: "768px") {
    .me {
    }
  }

  @media (min-width: "1366px") {
    .me {
      height: 100vh;
      width: 100%;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      background-color: var(--clr-floral-dark);
      .main {
        max-width: 900px;
        height: 100%;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        margin: 0 auto;

        h1 {
          font-size: 20rem;
          color: var(--clr-dark-bone);
          mix-blend-mode: luminosity;
          text-align: start;
          width: 4000px;
          position: absolute;
          top: 50%;
          left: 50%;
          transform: translate(-25%, -50%);
          z-index: 40;
        }

        h3 {
          width: 900px;
          font-size: 4.2rem;
          text-align: center;
          z-index: 100;
          color: var(--clr-bone);
          mix-blend-mode: soft-light;
          position: absolute;
          top: 50%;
          left: 50%;
          transform: translate(-50%, -50%);
        }
        #back {
          color: var(--clr-floral-dark);
          z-index: 40;
          mix-blend-mode: normal;
        }

        .img {
          max-width: 360px;
          img {
            max-width: 360px;
            display: block;
            position: absolute;
            border-radius: 0.8rem;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(12deg);
            z-index: 80;
          }
        }
      }
    }
  }

  @media (min-width: "1536px") {
    .me {
      .main {
        .name {
          h2 {
            font-size: 24rem;
            width: 5000px;
            left: 20%;
            transform: translate(-50%, -14%);
          }
        }
      }
    }
  }

  @media (min-width: "1920px") {
    .me {
      .main {
        max-width: 1200px;
        h2 {
          font-size: 26rem;
          width: 10000px;
          left: 20%;
          transform: translate(-20%, -10%);
        }

        h3 {
          width: 1200px;
          font-size: var(--fs-800);
        }

        .img {
          max-width: 420px;

          img {
            max-width: 420px;
          }
        }
      }
    }
  }
</style>
